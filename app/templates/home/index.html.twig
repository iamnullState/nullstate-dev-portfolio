{% extends "layouts/base.html.twig" %}

{% block content %}
<div class="row"> 
          <div class="col-lg-4 p-1 m-1"> 
            <h6>[ <span class="text-secondary">n3Dev</span> ] ⇢ <span class="text-secondary">microblog</span></h6>
            <p> 
              <span class="fs-6 fw-bold text-dark">{{ date }} </span>
            </p>
            <form method="post" action="/debug-session">
              <input type="hidden" name="_token" value="{{ csrf_token }}">
              <button type="submit">Test CSRF</button>
            </form>
            <form method="post" action="/debug-flash">
              <input type="hidden" name="_token" value="{{ csrf_token }}">
              type of alert : <br/>
              <input type="input" name="type" class="form-control">
              message : <br />
              <input type="input" name="message" class="form-control">
              <button type="submit">Test Flash Alert</button>
            </form>
          </div>
          <div class="col-lg-5 p-1 m-1"> 
            <h6>[ <span class="text-secondary">n3Dev</span> ] ⇢ <span class="text-secondary">blog</span></h6>
            
          </div>
        </div>  
{% endblock %}